{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Project Palantír",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}